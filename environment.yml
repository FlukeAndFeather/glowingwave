name: glowingwave
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - numpy>=1.24
  - pandas>=2.0
  - matplotlib>=3.7
  - seaborn>=0.12
  - scipy>=1.10
  - jupyter>=1.0
  - jupyterlab>=4.0
  - ipykernel>=6.0
  - pyyaml>=6.0
  - pip>=23.0
  # Geospatial libraries
  - geopandas>=0.14
  - cartopy>=0.22
  - requests>=2.31
  # Ocean data libraries
  - copernicusmarine
  - pip:
      # Add any pip-only packages here if needed
      # - some-package==1.0.0
